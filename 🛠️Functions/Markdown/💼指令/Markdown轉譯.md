將 [[Users/starchang/Documents/CloudFolder/GitHub/AI-Agent/🛠️Functions/Markdown/⌚️暫存/文章網址.md]] 的 URL 裡面的文章內容以 Markdown 格式產出於 [[Users/starchang/Documents/CloudFolder/GitHub/AI-Agent/🛠️Functions/Markdown/⌚️暫存/✅Markdown.md]] 文件內。

1. 每則文章的主標題一定都要以 `#`（h1）大標題開始。
2. 完整保留正文中的所有子標題（h2、h3、h4、h5 等），並依照 HTML 原始層級轉換為對應的 Markdown 格式（如 `##`、`###`、`####`、`#####`），不可遺漏任何段落標題。
3. 如果有多個連結，請依序排列文章內容（文章之間使用「<!-- ---------------- 文章分隔線 ---------------- -->」分隔）。
4. 必須去除導航列（nav）、側邊欄（sidebars）、廣告與無關雜訊（clutter）等不必要元素。